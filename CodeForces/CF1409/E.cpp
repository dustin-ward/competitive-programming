#include <bits/stdc++.h>
using namespace std;

int main() {
    int t; cin>>t;
    while(t--) {
        int n,k; cin>>n>>k;
        vector<int> X(n), Y(n), D(n);

        for(int i=0; i<n; i++)
            cin>>X[i];
        for(int i=0; i<n; i++)
            cin>>Y[i];

        sort(X.begin(), X.end());

        for(int i=X.size()-k; i>=0; i--) {
            int temp=0
        }
    }
}