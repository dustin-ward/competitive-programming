#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<vector<int>> grid(10, vector<int>(10));
    for(int i=0; i<10; i++) {
        for(int j=0; j<10; j++) {
            char c; cin>>c;
            grid[i][j] = c - '0';
        }
    }

    
}