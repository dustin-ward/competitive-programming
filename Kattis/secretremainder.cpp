#include <bits/stdc++.h>
using namespace std;

// Test case = 475249
const int M = 470;

int main() {
    int N; cin>>N;
    // if(N>=M) assert(false);
    // if(N>=lower)
    //     while(1);
    cout<<N%M<<endl;
}
